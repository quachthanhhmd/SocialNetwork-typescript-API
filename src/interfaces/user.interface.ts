
/**
 *  interface to manage feild when registering
 */
export interface CreateUserProfile {
    firstName: string,
    lastName: string,
    birthDay: Date,
    gender: string,
    userId: number,
}
