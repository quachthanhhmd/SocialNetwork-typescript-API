export interface IUserProfileAllField {
    id?: number,
    firstName: string,
    lastName: string,
    backgroundImage?: string | null,
    gender: string,
    birthDay: Date,
    avtImage?: string | null,
    displayName?: string,
    userId?: number
}